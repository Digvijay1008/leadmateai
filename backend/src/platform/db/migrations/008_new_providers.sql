-- ===========================================
-- Migration 008: Provider Cleanup Placeholder
-- Historical non-production providers were removed during hard cleanup.
-- No-op retained so migration ordering stays stable in local environments.
-- ===========================================

SELECT 1;
